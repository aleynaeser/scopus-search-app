import { SCTitle } from '@common/components/Layout';

export default async function NotFound() {
  return (
    <section id='not-found'>
      <SCTitle title='Page Not Found' />
    </section>
  );
}
